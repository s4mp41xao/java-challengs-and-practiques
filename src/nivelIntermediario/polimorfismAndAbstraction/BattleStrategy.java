package nivelIntermediario.polimorfismAndAbstraction;

public interface BattleStrategy {

    // declare the method
    // on interface for default the method is public
    void battleStrategyNinja();
}
